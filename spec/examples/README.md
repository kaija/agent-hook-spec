# Examples

- `claude-code/` contains baseline request and response payloads.
- `proposed/` contains complete payloads showing the proposed fields across `SessionStart`,
  `PreToolUse`, and `PostToolUse`.

Every JSON file in this directory is validated by `npm test`.
